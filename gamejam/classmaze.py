# import random, pygame, math
# from pygame.locals import *
# from classtext import ctext
# from classgrid import cgrid
# class cmaze():
    # def __init__(self, x, y, surf):
        # self.grid = cgrid(x, y, surf)
        # self.grid.fill(1)
        # self.pos_start = self.grid.getrandomcell(1) # 1 says choose a random wall cell 
        # self.pos_current = self.pos_start
        # self.visited = []
        
    #def create():
        #print("making maze")
        # self.visited.append(self.pos_start)
        # while len(self.visited) > 0:
            # neighbours = [(0,1),(1,0),(0,-1),(-1,0)]
            # for n in neighbours:
                #ineighbour = random.randint(0, len(neighbours))
                # oneighbour = self.pos_current + n
                # bskip = 0
                # for o in self.visited:
                    # if o == oneighbour:
                        # bskip = 1
                        # break
                # if bskip:continue 
                # self.grid.getgridvalue(oneighbour[0], oneighbour[1])
                
                
            
        
        # for y in range(0, self.grid.y - 1):
            # for x in range(0, self.grid.x - 1):
                
            